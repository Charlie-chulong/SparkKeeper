# 更新记录

## 3.0.3

- Setup 原位升级自动核对并暂停属于本安装目录的 Windows 每日任务；程序文件完整校验通过后恢复安装前的 Enabled，原停用保持停用、无任务不新建，不修改应用内计划配置。
- 普通原位升级无需先手动停用计划，但仍须正常结束当前任务并退出 GUI；禁用系统任务不会终止已有 worker，安装器遇到占用不强杀、不补跑。
- 新增同一 Windows 用户跨登录会话的维护互斥与持久维护记录。worker 在数据库初始化前取得独立维护锁并持有至收尾退出，GUI 选主后在数据库初始化期间检查维护状态；旧版不认识新保护，首次升级仍不得并发启动旧副本。
- 取消仅在未修改文件或已验证原文件完整时安全恢复任务；部分更新、崩溃、校验或任务恢复失败时保留维护记录并保持暂停，重新运行原目录的同版或更新版 Setup 修复。任务被人工改动时不覆盖定义、不重建已删除任务，验证完成后提示人工核对。
- 首次便携迁入不自动绑定异路径任务，须先人工停用旧计划，安装后在新版重新保存；ZIP 离线更新仍保留手动停用与人工启用流程，不使用 Setup 自动维护事务。
- 卸载改为明确确认后彻底删除本安装程序及当前用户 `%LOCALAPPDATA%\SparkKeeper` 全部共享数据，不提供保留选项；警告会影响同用户便携副本，静默卸载须显式 `/PURGEUSERDATA` 授权。仅清理严格归属本安装的任务，异路径任务、其他引用共享数据的关联计划或另一副本仍运行时中止、不擅改；正常更新与取消卸载不删除数据。清理失败保留原卸载器及维护记录，优先再次确认后重跑卸载，Setup 修复仅作兜底；不重建旧任务、不承诺数据恢复，完成凭证防止收尾中断后重复清理后来产生的数据。
- 本次维护机制不改变发送确认、当天防重复或非商业许可边界；不将安装维护验证宣称为消息实发验证，继续接受外部 PR。

## 3.0.2

- 新增用户级 Inno Setup 安装包 `SparkKeeper-3.0.2-Setup.exe` 及 SHA-256 校验，作为推荐下载；保留 `SparkKeeper-3.0.2-win64.zip` 备用分发与离线更新。
- 双击安装或升级，默认安装到 `%LOCALAPPDATA%\Programs\SparkKeeper`，固定 AppId 识别已有安装路径，不要求管理员权限。
- 安装、升级和卸载前须停用并保存每日计划、等待操作完成并正常退出；遇到占用不强杀进程，不自动补跑。
- 用户数据仍位于 `%LOCALAPPDATA%\SparkKeeper`，与程序目录分离；卸载保留数据。首次从便携目录迁入后需在新版重新保存每日计划并更新旧快捷方式。
- 明确未签名安装包的 SmartScreen 风险、项目非商业许可与 Inno Setup 自身许可的边界；不要求关闭安全软件，不承诺自动无损回滚，不修改旧标签或替换历史发布资产。

## 3.0.1

- 程序目录保持单行，窗口变窄时自动省略路径，悬停可查看完整目录；不再撑高底栏或挤压操作按钮。
- 项目自有代码自新增 `LICENSE` 的提交起采用 PolyForm Noncommercial License 1.0.0，仅按该许可授权非商业用途；第三方权利不变，历史标签与已发布资产不替换。
- 更新 README：当前功能、下载入口、合成数据界面截图、运行环境、升级步骤及常见问题。
- 明确接受外部 PR，增加贡献规范和 PR 模板；禁止提交真实登录态、聊天数据、数据库、未处理日志或私人截图。
- Windows 发行包携带根目录项目许可证，并纳入发行文件清单。

## 3.0.0

首次以统一正式版本号发布到 GitHub。

### 新增

- 同一 Windows 用户与会话的 GUI 单实例；重复启动请求唤醒已有窗口，支持最小化与模态弹窗。
- 界面显示统一版本及程序路径；构建、包名与 Git 标签使用单一版本来源。
- 离线 `更新.cmd` 入口、固定程序目录和 SHA-256 文件清单；完整替换程序与依赖，不合并遗留文件。
- 数据库未来版本拒绝、迁移前 SQLite 一致性备份和迁移事务保护。
- 人工 GitHub Release 预检与草稿发布工具，分发完整程序包、校验文件及 Qt 对应源码。

### 修复与保留

- 第二个 GUI 不再重复初始化业务数据；活跃自动化任务期间不把正在发送的尝试误恢复为不确定结果。
- 保留原生“续火花”表情与文本互斥、身份复核、当天防重复、未知结果不自动重发和随机等待。
- 保留整行选择、按选择状态显示好友操作、合并未知结果提醒以及独立应用图标。

### 升级注意

- 从旧开发版升级前，先停用每日计划，正常结束任务并关闭全部旧窗口；旧版没有单实例协议。
- 原先使用版本号目录的用户应迁入固定可写目录，随后重新保存每日计划。后续更新仍需先退出程序，更新不会强杀进程或自动补跑。
- 用户数据保留在 `%LOCALAPPDATA%\SparkKeeper`，不随程序目录替换。更新失败可恢复程序目录，但不能在新版产生业务记录后自动覆盖回旧数据库。
- 原生表情的历史只读取证与自动化行为测试不等同于在接收者账号上已完成实发验证；不保证网页结构、平台限制或火花规则永久不变。
