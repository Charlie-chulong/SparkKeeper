# 贡献指南

欢迎外部 Pull Request（PR）和问题反馈。请先阅读 [README](README.md)，确认项目的本地自用边界；不要提交验证码或风控绕过、群发营销等能力。

## 反馈与改动范围

- 问题反馈请提供程序版本、Windows/Python 版本（如适用）、最小复现步骤、预期与实际结果；只使用合成或彻底匿名的数据。
- 一个 PR 解决一个问题，说明动机、影响范围及不包含的改动，避免夹带重构或格式调整。
- 重大界面、架构、发送策略、数据格式或依赖变更，请先通过公开 Issue 讨论设计；讨论中也不得附带敏感材料。
- 修改可观察行为时，补充或更新针对性验证，并同步相关使用说明；涉及兼容性、数据库迁移或回退限制时，在 PR 中明确说明。

## 开发与验证

环境安装见 [README 的开发与构建说明](README.md#开发与构建)。使用项目虚拟环境与现有 pytest、ruff 配置，不依赖个人登录态或真实聊天数据完成测试。例如，数据库相关改动可运行：

```powershell
.venv\Scripts\python.exe -m pytest tests/test_database.py
.venv\Scripts\python.exe -m ruff check src tests tools
```

请将示例测试文件替换为与改动相关的现有测试；有未覆盖的新行为时增加针对性测试。界面或交互改动需记录实际操作验证；不得为验证而向真实好友自动发送消息。PR 中列出实际执行的命令、操作和结果，未执行的检查及原因也要如实说明，不要把计划当作已通过。

## 敏感数据：不得公开提交

以下材料不得进入提交及 Git 历史、PR 正文或附件、公开 Issue、评论或其他公开链接：

- 真实 Cookie、Token、密码、会话密钥及其他凭据。
- DPAPI 加密的真实登录态、浏览器 profile/用户数据目录；加密不代表可以公开。
- 本机运行产生的 SQLite 数据库及其旁路文件、数据库备份、诊断目录、日志或日志备份。
- 真实聊天正文、好友姓名/昵称、账号标识及其他可识别好友身份的信息。
- 未彻底处理的截图、录屏、任务 XML、配置或其他含隐私的文件。

原始日志在本地不脱敏保存，不等于可以公开上传。不要直接打包 `work/`、`outputs/` 或 `%LOCALAPPDATA%\SparkKeeper`；不要以 `.gitignore` 已忽略为唯一保证。提交前也要检查暂存内容、历史提交和附件。复现材料仅使用合成或彻底匿名的测试数据；不要只遮住截图中的名字，却保留正文、头像、账号、文件路径或其他可关联身份的内容。

若误提交敏感信息，立即停止转贴或在评论中重复展示，尽快撤回公开材料，并撤销或轮换相关凭据、失效相关登录会话；仅在后续提交中删除文件不能清除 Git 历史或已下载副本。请通过维护者已公开的私下联系方式联系维护者处理历史及附件；若尚无私下渠道，可仅公开请求联系方式，不附敏感内容。本项目不要求通过公开 Issue 提交敏感证据。

## 贡献权利与许可

本项目是源码公开、非商业授权项目；[PolyForm Noncommercial License 1.0.0](LICENSE) 不是 OSI 认可的开源许可证。该许可自新增 `LICENSE` 的提交起适用于项目自有代码，不追溯改变既有标签当时的许可状态。第三方组件及原生表情素材仍由各自权利人和原许可约束，见 [第三方声明](THIRD_PARTY_NOTICES.txt)。

提交贡献即表示你有权提供这些内容，并同意将贡献中的项目自有代码按本项目 PolyForm Noncommercial License 1.0.0 提供。请保留应有的版权和许可声明；不得复制无权贡献的第三方代码、移除第三方声明或把第三方内容改标为本项目许可。

贡献者保留其版权，无需转让版权，也不因提交 PR 而向维护者授予额外的商业使用或商业再许可权。本项目不要求签署 CLA 或提交 DCO 签名。若贡献受雇佣协议或他人权利约束，请先取得所需授权。
