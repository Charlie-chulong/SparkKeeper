# 续火花助手（Windows 本地自用版）需求

## 1. 目标

为每个 Windows 用户提供本地桌面工具，通过正常的抖音网页交互维护本人已授权好友的每日文本或原生“续火花”消息，不设应用层固定好友数量上限。

从 3.0.0 起统一版本并通过 GitHub Releases 分发 Windows 便携包，支持单实例与用户确认的离线整包更新。不包含收费、支付、软件授权、邀请码、代码签名或需要管理员权限的安装向导。

## 2. 使用边界

- 仅操作用户本人拥有或获明确授权的账号。
- 仅向明确同意接收测试消息的好友发送少量文本。
- 使用正常网页扫码、搜索、输入和发送流程。
- 不绕过验证码、安全验证或平台风控。
- 不实现浏览器指纹伪装、反检测、接口签名复刻或内部协议直发。
- 网页变化、登录失效、验证、限流和账号限制属于已知持续风险。

## 3. 首版范围

### 3.0 运行方式

- 软件直接在本机启动，不需要激活码、许可证服务或远程管理端。
- 网络只用于正常访问抖音网页；本地配置、历史和计划任务不依赖 CloudBase。

### 3.1 账号

- 仅支持一个抖音账号。
- 首次登录必须显示受控浏览器，由用户本人扫码。
- 登录后保存 Playwright Storage State，并使用当前 Windows 用户的 DPAPI 加密。
- 不复用日常 Chrome 或 Edge 用户目录。
- 自动任务默认后台运行；需要扫码或人工验证时停止任务，并在桌面程序下次打开时提示处理。

### 3.2 好友

- 保存已确认好友，不设固定数量上限。
- 用户输入关键词搜索，程序显示候选结果，用户确认具体好友后保存。
- 若聊天搜索只能检索已有会话或无法返回候选，允许用户在应用当前账号的可见受控浏览器中按备注名或昵称手动打开正确聊天，再由程序只读提取当前聊天身份。
- 捕获时填写右侧聊天顶部实际显示的准确名称；设置过好友备注时通常填写备注名，否则填写昵称。
- “读取当前聊天”不得输入文本、点击发送或自动选择同名用户；提取后仍须在桌面界面展示候选并由用户确认保存。
- 不假设抖音号可以进行稳定的全站用户搜索；抖音号仅在页面可见时作为身份核对证据。
- 昵称相同不得默认选择第一项。
- 优先保存网页可见的稳定身份信息，例如抖音号、个人主页地址或会话标识；昵称和头像只作为辅助展示。
- 每次发送前重新确认当前聊天对象。
- “已保存好友”点击任意列选中整行，键盘焦点覆盖整行，支持 Ctrl/Shift 多选。操作栏最多显示“全选/取消选择”切换按钮及“操作”菜单；空表隐藏操作，未选中隐藏菜单，菜单按所选启用状态显示适用的启停动作，部分选择时提供取消选择。任务期间所有管理动作禁用。
- 删除所选前确认人数、真实姓名与本地 ID；有发送历史的好友保留记录并停用，无历史者删除。批量管理在同一事务内完成，任一项失败整组回滚。
- 首版不支持群聊。

#### 火花好友扫描与导入

- 用户主动启动只读扫描，遍历当前网页可加载的聊天列表，不输入或发送消息。
- 默认勾选身份可靠的新 `LIGHT`、`GRAY` 和 `RECOVER` 好友；扫描后可自由勾选或取消任意条目，可靠单聊不受火花状态限制，可由用户明确选择导入。
- 昵称中的火焰、消息中的文字、图标颜色和天数不能单独作为有效火花证据。
- 预览显示扫描时间、数量、有效/待恢复/非有效/未知状态、已有记录与身份依据；待恢复提供独立计数和筛选，支持勾选及一次确认导入。
- 明确区分完整、部分和取消结果；无明确列表末端证据时不宣称已扫描全部。
- 扫描和导入均受跨进程互斥保护；扫描前后及导入事务内核对账号与登录代次。
- 新导入好友默认停用；按稳定身份去重，已有好友的资料、启用状态和历史保持不变，冲突或失败整批回滚。
- 不按昵称合并同名好友；身份冲突、群聊或身份尚未确认的选择会明确阻止整批导入，不静默过滤。火花状态未知仅作为提示，不替代身份核验。

### 3.3 消息

- 支持文本或抖音原生大表情“续火花”，二者互斥；所有启用好友使用同一种内容。
- 文本支持 Unicode、emoji 和多行；切换模式保留本地文本草稿，原生模式不发送该草稿。
- 原生模式显示真实资源预览，从网页表情面板选择准确资源；不是小表情“[续火花吧]”、同名文本或普通火焰 emoji。目标缺失、歧义或资源变化时明确停止，不自动退回文本。
- 不支持其他图片、视频、任意原生表情、随机模板或联网生成内容。

### 3.4 配置验证

提供“验证配置”操作，完成以下检查但不发送消息：

1. 恢复并检查登录状态。
2. 打开当前选定的聊天页面。
3. 搜索全部启用好友。
4. 确认每位好友搜索结果唯一且身份一致。
5. 打开聊天并确认输入框可用；原生模式仅打开表情面板并核对唯一目标，不点击表情。

新增好友、修改计划或页面异常后，用户可先执行验证配置。

### 3.5 手动发送

- 发送前显示账号、全部目标、内容类型和完整文本或原生表情名称。
- 用户对整批内容确认一次后，程序顺序处理目标。
- 默认在两次实际发送尝试之间随机等待 `3–8` 秒。
- 用户可设置 `0–120` 秒范围，且必须满足“最短 ≤ 最长”；`0–0` 表示关闭额外等待。
- 随机等待只降低连续发送频率，不保证避免平台限制或封禁。
- 单个目标失败时记录失败并继续后续目标。
- 登录失效、人工验证、风险提示或页面整体不可用时停止整批。
- 发送触发后不得自动重试。

### 3.6 发送结果

每次尝试必须记录以下终态之一：

- `success`：新的己方消息与本次文本或原生表情稳定资源匹配，发送中状态结束，且未出现失败标记。
- `failed`：有证据表明消息未成功发送。
- `unknown`：已经触发发送，但无法确定最终结果。
- `duplicate`：被当天防重复规则阻止。
- `cancelled`：发送前由用户或整体任务取消。

仅点击发送按钮、按下 Enter、点击原生表情或出现新气泡，均不足以判定成功。原生大表情点击即发送，点击前必须完成身份复核、基线采样与触发标记；触发后不再按文本发送按钮。

### 3.7 当天防重复

- 按“账号 + 好友稳定标识 + Windows 本地自然日”判断。
- 内容或消息类型变化均不影响当天防重复。
- 在触发发送前先以事务方式创建发送尝试。
- `success` 和 `unknown` 都阻止定时任务当天再次发送。
- 桌面程序允许用户在二次警告后人工覆盖。
- 人工覆盖必须记录时间、原记录和覆盖标志。
- Windows 定时 worker 永远不能自动覆盖。
- 清理展示历史不得静默清除当天防重复依据。

### 3.8 并发控制

- 桌面发送、配置验证、火花扫描/导入和定时 worker 共用同一个跨进程运行锁。
- 同一时间只能有一个网页自动化任务。
- 第二个任务不能排队后静默执行；应明确跳过并记录“已有任务运行”。

### 3.9 定时计划

- 每天在同一个用户配置时间执行。
- 所有启用好友共用该时间、消息类型与内容、好友间随机等待范围。
- 保存或修改计划时，用户确认目标、消息类型与内容和等待范围；到点后无需再次确认。
- 由桌面程序创建、更新和删除 Windows 计划任务。
- 计划任务使用绝对程序路径和绝对工作目录。
- 仅在当前 Windows 用户已登录时运行。
- 启用“唤醒计算机运行此任务”。
- 定时结束后发送 Windows 通知并保存结果。

### 3.10 错过计划

- 电脑关机、任务未运行或系统异常导致当天计划错过时，不自动补发。
- 下次打开桌面程序时显示原计划时间、目标、消息类型与内容和未发送对象；补发使用原快照，不套用后来修改的计划。旧快照按文本处理。
- 用户选择“补发”或“跳过”。
- 补发仍执行当天防重复检查。

### 3.11 本地数据

使用 SQLite 保存：

- 设置和页面适配器选择。
- 账号公开身份信息。
- 已确认好友。
- 消息类型和文本内容；旧计划、历史与待处理补发迁移后仍为文本。
- 定时计划和好友间随机等待范围。
- 批次和单目标发送尝试。
- 发送结果和错误分类。
- 人工覆盖审计。
- 待处理登录和错过计划提示。
- 应用事件日志。

完整消息文本按用户要求永久保留，直到用户主动清理。登录状态仍由 DPAPI 加密保存；不主动将 Cookie、Storage State 或请求凭据转储到 SQLite、日志或配置文件。事件原文按用户要求不再脱敏。

源码运行时，数据保存在项目 `work/local-data`；便携自用版运行时，数据保存在当前 Windows 用户的 `%LOCALAPPDATA%\SparkKeeper`，不得写入或随安装目录分发。便携压缩包不得包含制作者的数据库、登录态、好友、消息或任务 XML。

### 3.12 日志和诊断

事件和诊断按用户要求保存完整信息：

- 批次编号、真实好友名称及本地目标 ID，不再以“好友01”替代真实对象。
- 执行阶段、结果、错误分类和耗时。
- 原始异常类型、消息、原因链、诊断备注及调用栈；不按关键词过滤或截断。
- 查找失败时记录搜索框实际值、候选和匹配数量、身份来源与主要过滤原因，区分没有候选、身份不符和候选行失效。
- 界面提供可复制的完整事件原文；页面后台读取失败仍不弹阻塞对话框。

不主动转储登录凭据、请求头、整页 HTML 或聊天正文。原始异常可能包含本机路径和调用参数，事件仅在本机保存，由用户自行保管。历史上已经省略的错误上下文不能事后恢复。

失败截图和 Playwright Trace 默认关闭。用户主动开启一次性诊断时必须看到隐私警告。

## 4. 页面与错误处理

首选验证 `https://www.douyin.com/chat`。如果当前测试账号不能使用，再单独验证 Creator Center 聊天页面；首版只启用一条经验证的页面路线，不做自动跨页面降级。

错误至少分为：

- `authentication_required`
- `human_verification_required`
- `page_not_ready`
- `page_structure_changed`
- `target_not_found`
- `target_ambiguous`
- `target_identity_mismatch`
- `composer_unavailable`
- `send_failed`
- `send_unknown`
- `already_running`
- `duplicate_blocked`

页面导航和纯读取允许有限重试；发送触发后禁止自动重试。

## 5. 桌面界面

简单原生界面至少提供：

- 登录状态与扫码登录。
- 搜索、确认、启用、停用和删除好友。
- 在可见受控浏览器中手动打开聊天并只读捕获候选。
- 火花好友只读扫描、结果预览和批量导入（新增好友默认停用）。
- 固定文本编辑。
- 验证配置。
- 整批预览和手动发送。
- 当前进度和取消尚未触发的后续目标。
- 定时计划启用、时间修改和停用。
- 发送历史、运行日志和手动清理。
- 登录失效、结果不确定和错过计划提示。

耗时操作不得阻塞桌面界面事件循环。

## 6. 非目标

本版本不实现：

- 商业收费、支付、软件授权、激活码、邀请码和远程管理后台。
- 多账号。
- 联网自动更新、代码签名和带安装向导的正式安装程序；提供用户主动执行的离线整目录更新，不后台静默替换。
- GitHub Actions、Docker、传统云服务器或云托管容器。
- Webhook、钉钉、飞书等外部通知。
- 随机发送时间、随机输入节奏或其他反检测行为。
- 不保证发现网页未展示的全部账号联系人，不定期自动扫描或自动启用导入对象。
- 独立 CLI 发送入口。

## 7. 验收标准

1. 用户能扫码登录，重启应用后恢复会话，磁盘不存在明文 Storage State。
2. 用户能搜索并确认测试好友，昵称重复时不会误选。
3. 验证配置模式能完成全部只读检查，且不输入或发送消息。
4. 单好友文本发送只有在终态确认后标记 `success`。
5. 崩溃或超时后的尝试标记 `unknown`，定时任务当天不自动重发。
6. 全部启用好友顺序执行，目标级错误继续，登录或验证错误停止整批。
7. 手动和定时任务不能并发运行。
8. 当天重复默认拦截；仅手动二次确认可覆盖，且留下审计记录。
9. 每日计划可由 Windows 计划任务触发，并在当前用户登录时后台运行。
10. 错过计划不会擅自补发，下次打开应用会询问。
11. Windows 通知仅显示批次统计；本地事件保留真实对象与完整错误详情，账号凭据仍加密且不主动转储。
12. 普通日志和默认失败路径不产生截图或 Trace。
13. 便携 ZIP 在未安装 Python 的 Windows 用户环境中可以启动桌面界面和内置 Chromium，首次运行不包含任何制作者运行数据。
14. 同用户同登录会话正常 GUI 启动只保留一个窗口，次实例仅请求激活；在选主前不得初始化或修改业务数据，独立 worker 不使用 GUI 生命周期锁。
15. 版本由 `__version__` 单一来源驱动，界面、发行包与 Git 标签一致；更新包内程序目录固定为 `SparkKeeper`。
16. 离线更新检查完整性、路径、程序占用与停用计划，完整替换程序目录；失败保留或恢复原目录，不覆盖用户数据、不强杀进程、不自动补跑。
17. 数据库升级前一致性备份，未知更高 schema 拒绝修改；迁移失败保留原数据与备份，活跃自动化任务中的 sending 不得被启动恢复误标 unknown。
18. GitHub 公开源码排除运行数据和凭据；Release 人工确认发布，附 ZIP 校验及对应第三方源码。暂不启用 Actions 自动构建发布。
