# 续火花助手（开发版）实现任务

## A. 工程与数据

- [x] 创建 `pyproject.toml`、`src` 包和应用入口。
- [x] 统一项目路径；冻结版运行数据隔离到 `%LOCALAPPDATA%\SparkKeeper`。
- [x] 建立 SQLite schema 和幂等迁移。
- [x] 实现账号、好友、计划、批次、发送尝试、去重、事件和待处理事项仓储。
- [x] 实现残留 `sending` 状态恢复为 `unknown`。
- [x] 实现 DPAPI 加密、解密和原子写入。
- [x] 实现 Windows 命名互斥量。

完成条件：数据库重复初始化安全，DPAPI 往返成功，两个进程不能同时持有任务锁。

## B. 浏览器和登录

- [x] 建立 Playwright 异步浏览器会话。
- [x] 实现可见扫码登录并把 Storage State 直接加密保存。
- [x] 实现后台会话恢复。
- [x] 对普通 `/chat` 页面执行能力探测。
- [x] 实现登录、人工验证、页面未就绪和结构变化分类。
- [x] 实现白名单页面诊断。

完成条件：登录状态不落明文；重启后可恢复；错误分类不会把页面正文写入日志。

## C. 好友与验证配置

- [x] 实现关键词搜索和候选提取。
- [x] 排除群聊并为候选生成稳定身份键。
- [x] 在桌面界面展示候选并要求人工确认。
- [x] 保存 1–5 位好友及身份证据。
- [x] 实现重新搜索、打开聊天和收件人二次确认。
- [x] 实现只读验证配置批次。

完成条件：同名候选不会自动选中；验证配置不会输入或发送消息。

## D. 发送和批次

- [x] 实现输入文本并核对编辑器内容。
- [x] 实现发送前事务占位。
- [x] 实现己方新消息识别。
- [x] 实现发送中、成功、失败和超时终态机。
- [x] 把无法确认的触发后异常持久化为 `unknown`。
- [x] 实现同账号、好友和本地日期去重。
- [x] 实现只允许手动二次确认的覆盖。
- [x] 实现 1–5 位好友串行批次。
- [x] 目标错误继续，认证和验证错误停止。
- [x] 实现取消尚未触发的后续目标。

完成条件：成功、失败和未知状态有可观察证据；触发发送后无自动重试；崩溃恢复不会自动重发。

## E. 桌面程序

- [x] 实现账号登录区。
- [x] 实现好友搜索、确认和管理区。
- [x] 实现固定文本和整批预览。
- [x] 实现验证配置、手动发送和运行进度。
- [x] 实现每日计划配置。
- [x] 实现发送历史和事件日志。
- [x] 实现安全的历史清理。
- [x] 实现登录、未知结果和错过计划提示。
- [x] 浏览器任务运行于后台线程，不阻塞 tkinter。
- [x] 实现 Windows 批次结果通知。

完成条件：所有用户操作可从桌面界面完成；界面在浏览器运行时保持响应。

## F. Windows 定时执行

- [x] 生成并校验 Task Scheduler XML。
- [x] 创建、更新、查询和删除计划任务。
- [x] 配置当前用户交互令牌、最低权限、唤醒和禁止恢复即运行。
- [x] 实现 `spark_keeper.worker --scheduled`。
- [x] worker 复用批次服务并禁止人工覆盖。
- [x] 实现最近一次错过计划检测。
- [x] 实现“补发”或“跳过”处理。

完成条件：任务动作使用绝对路径和工作目录；定时与手动任务无法并发；错过后不会擅自补发。

## G. 验证

- [x] 为数据迁移、发送占位、崩溃恢复和去重添加测试。
- [x] 为 DPAPI 和任务互斥添加 Windows 测试。
- [x] 为候选身份、页面分类和安全诊断添加测试。
- [x] 为发送终态机添加时间线测试。
- [x] 为批次停止/继续策略添加测试。
- [x] 为计划 XML和错过检测添加测试。
- [x] 运行聚焦测试套件。
- [x] 启动真实桌面入口进行行为冒烟。
- [x] 使用测试账号完成扫码、验证配置和单条消息验收。

最后一项需要用户本人扫码并确认测试好友；其余验证由开发环境自动完成。
